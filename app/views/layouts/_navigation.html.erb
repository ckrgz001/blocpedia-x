<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "*Blocpedia-X*", root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Wikis", wikis_path %></li>
        <li><%= link_to "Users", users_path %></li>
        <li><%= link_to "New Wiki", new_wiki_path %></li>
      <% if user_signed_in? && current_user.role == 'standard'%>
         <li><%= link_to "Upgrade to Premium!", new_charge_path(current_user), class: 'btn btn-warning btn-sm' %> </li>    
          <% end %>
      </ul>
      
         <div class="pull-right user-info">
        <% if user_signed_in? && current_user.role == 'standard'%>
          <%= image_tag current_user.avatar_url(48), class: "gravatar" %>
          <div class="pull-right logged-in">
            <%= link_to current_user.username, user_path(current_user), class: "navnav" %><br>
            <%= link_to "Sign Out",  destroy_user_session_path, :method => :delete , class: "navnav"%>
          </div>
        <% elsif user_signed_in? && current_user.role == 'premium' %>
          <%= image_tag current_user.avatar_url(48), class: "gravatar" %>
          <div class="pull-right logged-in">
            <%= link_to current_user.username, user_path(current_user), class: "navnav" %> <%= image_tag asset_url('Untitled.png') %><br>
            <%= link_to "Sign Out",  destroy_user_session_path, :method => :delete , class: "navnav"%>
          </div>
        <% elsif user_signed_in? && current_user.role == 'admin' %>
          <%= image_tag current_user.avatar_url(48), class: "gravatar" %>
          <div class="pull-right logged-in">
            <%= link_to current_user.username, user_path(current_user), class: "navnav" %> <%= image_tag asset_url('tcat.png') %><br>
            <%= link_to "Sign Out",  destroy_user_session_path, :method => :delete , class: "navnav"%>
          </div>
        <% else %>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to 'Log In',  new_user_session_path%></li>
            <li><%= link_to ' Sign Up', new_user_registration_path %></li>
          </ul>
        <% end %>
      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>